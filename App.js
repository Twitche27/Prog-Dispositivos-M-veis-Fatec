import { ScrollView} from 'react-native';
import Contador from './pages/Contador';

function App() {
  return (
    <ScrollView style={{flex : 1}}>
      <Contador />
    </ScrollView>
  );
}

export default App;
