import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  secaoContainer: {
    marginBottom: 10,
    marginTop: 10,
    alignItems: 'center',
    justifyContent: 'center'
  },
  numero: {
    fontSize: 40,
    fontWeight: 'bold',
    textAlign: 'center'
  }
});
