import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    marginBottom: 10,
  },
  label: {
    fontWeight: 'bold',
  },
  picker: {
    borderWidth: 1,
    borderRadius: 5,
  }
});
