import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: 300,
    height: 300,
    alignSelf: 'center',
    marginTop: 15,
    marginBottom: 15,
  },
});

export default styles;
