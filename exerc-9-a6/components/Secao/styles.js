import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  secaoContainer: {
    marginBottom: 10,
    marginTop: 10,
    justifyContent: 'center'
  },
  texto: {
    fontSize: 20,
    fontWeight: 'bold',
    
  }
});
